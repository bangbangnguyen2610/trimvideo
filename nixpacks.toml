# Nixpacks configuration for Railway
# This ensures ffmpeg is installed for video processing

[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn webhook_receiver:app --host 0.0.0.0 --port ${PORT:-8000}"
